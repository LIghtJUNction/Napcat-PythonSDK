修复了多余导入，包括logging模块。将类型标注从typing模块的弃用类型（如List、Optional）更改为内置类型（list、Type | None）。将Union的使用更改为|。根据OpenAPI规范，修复了所有内部组件模型（如消息forward、markdown消息等），包括其data字段的具体结构，并将它们重命名为符合PEP8规范的驼峰命名法（如ForwardMessage、MarkdownMessage）。为每个消息类型定义了其内部Data模型。处理了Literal类型的使用，特别是针对枚举值（如status、sex、role）和const字段（如消息类型type）。处理了循环依赖问题，使用`from __future__ import annotations`和字符串字面量进行类型引用。将GetRecentContactRes的Data字段类型定义为内部类RecentContactItem，并根据API响应结构详细定义了其字段。确保了所有字段都带有描述信息和适当的默认值。移除了未使用的logger。